- model_class = Customer
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize.titleize
=will_paginate(@customers)
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:name)
      %th= model_class.human_attribute_name(:city)
      %th= model_class.human_attribute_name(:state)
      %th= model_class.human_attribute_name(:country)
      %th= model_class.human_attribute_name(:mphone)
      %th= model_class.human_attribute_name(:email)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @customers.each do |customer|
      %tr
        %td= link_to customer.id, account_customer_path(customer)
        %td= customer.name
        %td= customer.city
        %td= customer.state
        %td= customer.country
        %td= customer.mphone
        %td= customer.email
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_account_customer_path(customer), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), account_customer_path(customer), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_account_customer_path, :class => 'btn btn-primary'
