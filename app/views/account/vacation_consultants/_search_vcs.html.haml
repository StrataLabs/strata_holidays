%table.table.table-stripped#search_vc_table
  %tbody
    -if @vcs.empty?
      No Results Found
    -else
      -@vcs.each_with_index do |vc,index|
        %tr{:id => "#{vc.id}"}
          %td.cb
            %input{:type => "checkbox", :value => "#{vc.id}"}/
          %td
            %img{:src => '/assets/unwinders/divya.jpg'}
          %td
            = link_to vc.name, vacation_consultant_path(vc.id)
            %br
            = "As a vacation consultant, #{vc.name} living in #{vc.city}, is happy to engage with customers living in neighborhoods <1>, <2>, <3> of the <city> to offer <planning> & <booking> services.Customers who have signed up with #{vc.name} for their vacation requirements in the past, have this to say. <Click here> for testimonials. Of the destinations that #{vc.name} has planned, the popular ones have been <Dest1>, Dest2>, <Dest3>. <Click here> to see details of the body of work of #{vc.name}"
          / %td=vc.name
          / %td=vc.email
/= link_to 'Select', '#', :class => ['btn', 'btn-primary', 'select-vcs'], :remote => true

:javascript
  $('.select-vcs').on('click', function () {
    var vc_ids = [];
    $('#search_vc_table tr').filter(':has(:checkbox:checked)').each(function() {
      vc_ids.push($(this).attr("id"));
      console.log($(this).attr("id"));
    });

    return false;
  });