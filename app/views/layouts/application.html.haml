!!!
%html
  %head
    %title Unwinders
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = stylesheet_link_tag "unwinders/application", :media => "all"
    = csrf_meta_tags
  %body{:style => 'padding-top: 0px;background:#1f1b10'}
    = render 'layouts/header'
    #banner
      %h1
      #menu
      #submenu
        %ul
          %li= link_to "Home", root_path
          %li= link_to "Profile", user_unwinders_path
          %li= link_to "VC", vc_unwinders_path(:id => 1)
          %li= link_to "Signin", user_session_path
          %li= link_to "Signup", "#"
          %li= link_to "About Us", '#'
          %li= link_to "Contact Us", '#'
    /#home-page-logo
    - flash.each do |name, msg|
      = content_tag :div, msg.html_safe, :id => "flash_#{name}", :class => 'alert' if msg.is_a?(String)
    = yield
/     = render 'layouts/footer'
